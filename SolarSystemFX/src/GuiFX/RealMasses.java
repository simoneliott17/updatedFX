package GuiFX;

public class RealMasses {

    final static double SUN_MASS = 1.989 * Math.pow(10,30);
    final static double MERCURY_MASS = 3.3011 * Math.pow(10,23);
    final static double VENUS_MASS = 4.867 * Math.pow(10,24);
    final static double EARTH_MASS = 5.972 * Math.pow(10,24);
    final static double MARS_MASS = 6.39 * Math.pow(10,23);
    final static double JUPITER_MASS = 1.898 * Math.pow(10,27);
    final static double NEPTUNE_MASS = 1.02413 * Math.pow(10,26);
    final static double SATURNE_MASS = 5.6834 * Math.pow(10,26);
    final static double URANUS_MASS = 86.813 * Math.pow(10, 24);
}
